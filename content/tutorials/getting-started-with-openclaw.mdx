---
title: "OpenClaw Setup Guide: Step-by-Step Installation for Mac, Windows & Linux (2026)"
description: "A comprehensive guide to installing and configuring OpenClaw on any platform. From prerequisites to your first running instance."
date: "2026-02-15"
lastModified: "2026-05-01"
author: "Vibe OpenClaw Team"
tags: ["setup", "beginner", "installation"]
category: "getting-started"
howTo:
  - name: "Install Node.js 22+"
    text: "OpenClaw requires Node.js 22 or newer. Install via the official installer, nvm, or your package manager. Verify with `node --version`."
  - name: "Install OpenClaw"
    text: "Run `npm install -g openclaw` (or `brew install openclaw` on macOS). The CLI is then available as `claw`."
  - name: "Run onboarding"
    text: "Run `claw onboard` in your terminal. The wizard walks through provider selection, API key entry, and channel setup."
  - name: "Pick an LLM provider"
    text: "Choose Anthropic, OpenAI, Google, or a local model via Ollama. Paste your API key — OpenClaw stores it securely in ~/.openclaw."
  - name: "Chat with your agent"
    text: "Run `claw chat` or open http://localhost:3000 to start your first conversation. From here you can install skills and connect MCP servers."
faqs:
  - question: "How to setup OpenClaw step by step?"
    answer: "Install Node.js 22+, run `npm install -g openclaw`, then run `claw onboard` and follow the wizard. The onboarding flow handles provider selection, API key configuration, channel setup, and your first skills. Total setup time is about 10 minutes if you have an LLM API key ready."
  - question: "How to use OpenClaw on Windows?"
    answer: "Install Node.js for Windows, then run `npm install -g openclaw` from PowerShell or Windows Terminal. OpenClaw runs natively on Windows 10 and 11. For Docker-based setup on Windows, use Docker Desktop with WSL2 enabled."
  - question: "How do I install OpenClaw from GitHub?"
    answer: "Clone the repo with `git clone https://github.com/openclaw/openclaw.git`, run `cd openclaw && npm install`, then `npm link` to install the CLI globally. This is recommended only if you plan to contribute or modify OpenClaw — for normal use, install via npm."
  - question: "Can I run OpenClaw locally?"
    answer: "Yes — OpenClaw is designed to run locally. The agent, gateway, and skills all run on your machine. Only LLM inference goes to your provider (or stays local if you use Ollama). No data is sent to OpenClaw servers."
  - question: "How to setup OpenRouter for OpenClaw?"
    answer: "During `claw onboard`, choose 'Custom' as your provider and enter https://openrouter.ai/api/v1 as the base URL with your OpenRouter API key. OpenClaw treats OpenRouter as an OpenAI-compatible endpoint, giving you access to all OpenRouter models."
---

## What is OpenClaw?

OpenClaw is the fastest-growing open-source AI agent platform, designed to run autonomous tasks on your machine. With over 191,000 GitHub stars and 21,000+ active instances worldwide, it has rapidly become the go-to framework for personal AI automation.

This guide walks you through installing OpenClaw on macOS, Windows, and Linux, then configuring it for first use.

## Prerequisites

Before installing OpenClaw, make sure you have:

- **Node.js 20+** (or Bun 1.1+)
- **Git** installed and configured
- **4GB RAM minimum** (8GB recommended)
- An API key from a supported LLM provider (OpenAI, Anthropic, or local models via Ollama)

## Installation on macOS

The fastest way to install on macOS is via Homebrew:

```bash
brew install openclaw
```

Alternatively, you can install from source:

```bash
git clone https://github.com/openclaw/openclaw.git
cd openclaw
npm install
npm run build
npm link
```

Verify the installation:

```bash
claw --version
```

## Installation on Windows

For Windows, we recommend using the official installer or WSL2.

**Option 1: Official installer**

Download the latest `.msi` installer from the OpenClaw releases page and run it. The installer adds `claw` to your PATH automatically.

**Option 2: WSL2**

If you use Windows Subsystem for Linux, follow the Linux instructions below inside your WSL2 environment.

**Option 3: npm**

```bash
npm install -g openclaw
```

## Installation on Linux

Most Linux distributions can install via npm:

```bash
npm install -g openclaw
```

For Debian/Ubuntu, there is also an official APT repository:

```bash
curl -fsSL https://repo.openclaw.dev/gpg | sudo gpg --dearmor -o /usr/share/keyrings/openclaw.gpg
echo "deb [signed-by=/usr/share/keyrings/openclaw.gpg] https://repo.openclaw.dev stable main" | sudo tee /etc/apt/sources.list.d/openclaw.list
sudo apt update && sudo apt install openclaw
```

## Initial Configuration

Once installed, run the setup wizard:

```bash
claw init
```

This walks you through:

1. **Choosing an LLM provider** — Select OpenAI, Anthropic, or a local model
2. **Setting your API key** — Stored securely in `~/.config/openclaw/config.yaml`
3. **Configuring permissions** — Choose what OpenClaw can access (file system, network, etc.)
4. **Installing starter skills** — Optionally install recommended skills

## Verifying Your Setup

Run a quick test to make sure everything works:

```bash
claw run "What is 2 + 2?"
```

If you see a response, congratulations — OpenClaw is up and running!

## Recommended Next Steps

- Browse our [Skills Directory](/skills) to find useful skills
- Read our guide on [Installing and Managing Skills Safely](/tutorials/install-manage-openclaw-skills)
- Join the OpenClaw Discord community for support

## Troubleshooting Common Issues

**"Command not found" after installation**

Make sure your npm global bin directory is in your PATH. Run `npm bin -g` to find the path, then add it to your shell profile.

**API key errors**

Double-check your API key in `~/.config/openclaw/config.yaml`. Make sure there are no trailing spaces or line breaks.

**Permission denied errors**

On Linux/macOS, you may need to adjust file permissions:

```bash
chmod 600 ~/.config/openclaw/config.yaml
```

**High memory usage**

If OpenClaw is using too much RAM, try reducing the context window size in your config:

```yaml
model:
  max_context: 4096
```

## Further Reading

- [How to Run OpenClaw in Docker](/tutorials/openclaw-docker-setup) — Containerized setup with Docker Compose for production-grade deployments
- [OpenClaw API Tutorial](/tutorials/openclaw-api-tutorial) — Build custom integrations with the REST API and webhooks
- [OpenClaw MCP Server Guide](/tutorials/openclaw-mcp-server-guide) — Connect 1000+ tools via the Model Context Protocol
- [Self-Hosting OpenClaw](/tutorials/openclaw-self-hosting-guide) — Docker Compose, SSL, Tailscale, and security hardening
- [How to Install and Manage OpenClaw Skills Safely](/tutorials/install-manage-openclaw-skills) — Learn to find, install, and vet skills securely
- [OpenClaw WhatsApp Bot Setup](/tutorials/openclaw-whatsapp-bot-setup) — Connect OpenClaw to WhatsApp for AI-powered messaging
- [Is OpenClaw Safe?](/blog/is-openclaw-safe) — A complete security guide for configuring OpenClaw safely
